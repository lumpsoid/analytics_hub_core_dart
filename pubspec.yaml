name: analytics_hub_core
description: >
  Core analytics abstractions, fan-out, middleware pipeline,
  consent, session management, and offline queue interface.
  Pure Dart — no Flutter, no platform channels, no storage.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  mocktail: ^1.0.5
  very_good_analysis: ^10.2.0
dependencies:
  uuid: any
  meta: any
  test: any
